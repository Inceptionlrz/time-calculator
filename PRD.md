# 产品需求文档（PRD）：工作日 / 日历日截止日期计算器

| 字段 | 内容 |
| --- | --- |
| 文档版本 | v1.0（草稿） |
| 产品经理 | 许清楚 |
| 创建日期 | 2026-06-17 |
| 项目代号 | `deadline_calculator` |
| 技术栈 | Vite + React + Tailwind CSS（纯前端 Web 应用，默认） |
| 文档状态 | 评审中（产品决策 DR-1~DR-3 已确认） |

---

## 1. 产品目标（Product Goals）

1. **准确换算**：在给定起始日期、时间类型（日历日 / 工作日）和时长后，自动、准确地计算出截止日期，正确处理周末、法定节假日与调休补班日。
2. **降低认知负担**：将"3 个日历日""5 个工作日内"等口语化、易混淆的表述，转化为明确的日期节点与可解释的明细，减少人工推算错误。
3. **可解释与可配置**：输出不仅给出结果日期，还给出计算过程明细（跨越的周末 / 节假日 / 补班日数量），并支持按地区与单休场景灵活切换日历规则。

---

## 2. 用户故事（User Stories）

1. **As a** 职场员工，**I want** 输入"今天起 5 个工作日内提交"能立刻得到具体截止日期，**so that** 我不会因周末和假期算错而误期。
2. **As a** 项目协调人，**I want** 同时对比"日历日""工作日"两种口径下的截止日期，**so that** 我能向不同方（客户 / 内部）给出一致且无歧义的时间承诺。
3. **As a** 单休制公司员工，**I want** 按所在公司实际休息日（如仅周日休息）计算工作日，**so that** 计算结果贴合我的真实排期。
4. **As a** 合规 / 财务岗用户，**I want** 查看计算明细（含哪些天是法定假、哪些是补班），**so that** 我能向审计或上级解释时间依据。
5. **As a** 跨地区团队用户，**I want** 切换节假日规则地区（如中国大陆 / 香港），**so that** 海外分支的截止日同样准确。

---

## 3. 需求池（优先级）

### P0（Must Have，首版必备）
- **P0-1** 支持"日历日 / 自然日"口径计算：截止日 = 起始日 + N − 1 天（N 个自然日内，含起始日），并输出具体日期。
- **P0-2** 支持"工作日"口径计算：以周一至周五为基准工作日，自动跳过周末，并排除国家统一法定节假日。
- **P0-3** 内置中国大陆法定节假日与调休补班日数据集，并正确将"调休上班的周末"计入工作日。
- **P0-4** 输出结构化结果：截止日期（YYYY-MM-DD）、剩余日历天数、途经周末天数、途经法定节假日天数、途经补班天数。
- **P0-5** 输入校验：起始日期必填且为合法日期；时长 N 为正整数；时间类型必选。

### P1（Should Have，首版建议）
- **P1-1** 反向 / 剩余计算（两种语义，**首版均交付**，详见决策记录 DR-3）：
  - **截止日反推（核心语义）**：给定截止日期与口径，反推"还需多少个工作日 / 日历日"（与 P0-4「剩余日历天数」倒计时同源，服务于倒计时类用户故事）。对应函数 `countDaysBetween(start, deadline, type)`。
  - **反向偏移（已有能力，保留）**：给定起始日与 N，求"起始日 − N 天"的日期（现有 `direction='backward'`）。作为"往前推 N 天"的独立工具保留，命名与界面须与上述"截止日反推"严格区分，避免混淆。
- **P1-2** 单休 / 自定义工作日历：允许用户配置每周休息日（如仅周日休息），单休场景下周六计入工作日，但法定节假日仍按国家统一规则排除。
- **P1-3** 地区切换：支持中国大陆、中国香港等地区的节假日数据集。
- **P1-4** 起始日落在周末 / 节假日时的计数规则可配置（见边界情况与待确认问题）。
- **P1-5** 结果可复制 / 导出为文本或图片，便于粘贴到邮件 / IM。

### P2（Nice to Have，后续增强）
- **P2-1** 批量计算：一次输入多组（起始日 + 口径 + 时长），表格化输出。
- **P2-2** 智能解析：直接粘贴"请于 3 个工作日内提交"等自然语言，自动识别起始日 / 口径 / 时长。
- **P2-3** 日历可视化：在月历上高亮起止日与途经的非工作日。
- **P2-4** 数据自动更新：对接官方节假日公告 API，自动拉取未来年份安排。

---

## 4. 功能详细说明

### 4.1 输入（Input）
| 参数 | 必填 | 说明 | 默认 / 约束 |
| --- | --- | --- | --- |
| 起始日期 | 是 | 任务下达或约定的起始日 | 合法日期，格式 YYYY-MM-DD |
| 时间类型 | 是 | 枚举：`calendar`（日历日/自然日）、`workday`（工作日） | 二选一 |
| 时长 N | 是 | 要求的天数 | 正整数（N ≥ 1） |
| 是否含起始日 | 否 | 计数是否把起始日算作第 1 天 | 默认"含"（见 4.4 边界） |
| 地区 / 日历规则 | 否 | 节假日数据集与每周休息日配置 | 默认"中国大陆 · 双休" |
| 截止日期（反向模式） | 条件必填 | P1-1 反向计算时使用 | 与"时长 N"二选一 |

### 4.2 输出（Output）
- **截止日期**：`YYYY-MM-DD`，并附星期几。
- **剩余日历天数**：从"今天"到截止日的天数（用于倒计时提示）。
- **计算明细**：途经的周末天数、法定节假日天数、调休补班天数（均列出具体日期）。
- **口径对照**（可选）：同一 N 在日历日 / 工作日两种口径下的截止日对比。

### 4.3 计算规则（核心逻辑）
1. **工作日定义**：周一至周五为工作日；周六、周日为周末（默认双休）。
2. **法定节假日排除**：落在国家统一法定节假日范围内的日期，无论周几，均不计入工作日。
3. **调休补班计入**：因法定节假日调休而安排在周末的"补班日"，视为正常工作日，计入工作日。
4. **日历日 / 自然日**：两者同义，连续计数，不跳过任何日期（含周末与节假日）。
5. **工作日计数**：从起始日（若含起始日且起始日为工作日，则计为第 1 个工作日）开始，逐日向后，仅对"工作日"累加计数，直到累计达到 N，该日即为截止日。
6. **单休处理原则**：用户所述"单休问题按法定假日处理"——默认仍以标准双休工作日历为基准；当开启单休模式时，仅周日休息、周六上班（计入工作日），但所有法定节假日仍统一按国家规则排除（不因为单休而多算或少算法定假）。

### 4.4 边界情况（Edge Cases）
| 场景 | 处理规则 |
| --- | --- |
| 起始日恰为周末 / 法定节假日 | 若"含起始日"且起始日非工作日，则从下一个工作日起算第 1 个工作日（规则可由 P1-4 配置为"起始日强制计入"）。 |
| N = 0 | 校验拦截，提示 N 须为正整数。 |
| 跨年计算 | 数据集须覆盖跨年区间，自动衔接。 |
| 未来年份节假日未公布 | 标注"该年份法定节假日数据待官方公布，当前按历史规则估算"，并给出提示。 |
| 调休补班日落在周末 | 该日强制计入工作日，不计入"周末天数"明细。 |
| 单休 + 法定假重叠 | 法定假优先排除，不因单休将其转为工作日。 |
| 起始日 = 截止日 | 当 N=1 且含起始日时，截止日 = 起始日。 |

---

## 5. 非功能需求（Non-Functional Requirements）

- **准确性（最高优先）**：工作日计算须 100% 匹配官方节假日 / 补班数据；提供单元测试覆盖跨年、调休、单休等典型用例。
- **性能**：单次计算 < 50ms（数据集规模内）；批量计算（P2-1）千条以内 < 1s。
- **可维护性**：节假日数据从代码逻辑中解耦，以结构化数据文件（如 JSON）维护，便于年度更新。
- **可用性**：结果页面一眼可见截止日与星期；移动端自适应。
- **可解释性**：每次结果都附带明细，避免"黑盒"。
- **国际化（P2）**：界面文案支持中英文切换。
- **数据安全**：纯前端计算、无用户隐私数据上传（若后期引入云端同步则另议）。

---

## 6. 待确认问题（Open Questions）

1. **"含起始日"口径**：业界对"N 个工作日内"是否把起始日算作第 1 天存在歧义。默认采用"含起始日"，是否需提供开关？
2. **单休语义确认**：用户说"单休按法定假日处理"，是否意味着——单休仅改变每周休息日（周日），而法定节假日规则全国统一？请确认此理解。
3. **节假日数据来源**：首版使用内置静态 JSON（覆盖近 3 年 + 当前年），还是直接对接官方公告 API（P2-4）？
4. **是否需要反向计算（P1-1）与批量（P2-1）进入首版范围？**
5. **地区范围**：首版是否仅需"中国大陆"，还是同步支持香港 / 澳门等？

---

## 7. 验收标准（Acceptance Criteria）

- **AC-1**：输入起始日 `2026-03-04`（周三）、类型 `workday`、N=5，系统输出截止日 `2026-03-10`（周二，跳过 03-07 周六与 03-08 周日两个周末），明细显示途经周末 2 天、途经法定节假日 0 天；结果正确。（注：原示例 `2026-06-17`+5 工作日因覆盖端午法定假 06-19 实际得 `2026-06-24`，已改为无假期干扰窗口，详见决策记录 DR-1。）
- **AC-2**：输入起始日 `2026-06-17`、类型 `calendar`、N=3，系统输出截止日 `2026-06-19`（周五），明细显示途经 0 个周末（自然日不跳过）。
- **AC-3**：当起始日或途经日落在已配置的法定节假日（如国庆 10-01~10-07）时，该区间不被计入工作日；当途经调休补班日（如某周六补班）时，该日被计入工作日。
- **AC-4**：单休模式下，周六计入工作日、周日休息；法定节假日仍被排除。
- **AC-5**：非法输入（空起始日、N ≤ 0、非日期格式）被拦截并给出明确错误提示，不产生错误结果。
- **AC-6**：结果页面同时展示截止日期、星期、剩余天数与计算明细，且可在移动端正常显示。
- **AC-7**：核心计算逻辑具备单元测试，覆盖 P0 全部规则与 4.4 各边界场景，测试通过率 100%。

---

---

## 8. 决策记录（Decision Record）

> 由产品经理（许清楚）在架构评审后确认，覆盖架构师提出的 3 项需产品侧拍板的问题。所有决策已回写至对应章节，工程师可据此开工。

### DR-1：AC-1 验收示例冲突（采用方案 A）

- **问题**：原 AC-1 期望 `2026-06-17`+5 工作日 → `2026-06-23`；但 2026 端午法定假 `2026-06-19`（周五）被算法正确跳过，实际结果为 `2026-06-24`。算法无误，属 PRD 示例与真实节假日数据冲突。
- **决策**：**修改 AC-1 示例为一个无假期干扰的窗口** —— 改用 `2026-03-04`（周三）起 5 工作日 → `2026-06-23` 误，实际应为 `2026-03-10`（周二），途经周末 2 天、法定节假日 0 天。该窗口不含任何法定节假日，验收基准与算法结果一致。
- **理由**：方案 A 比"标注忽略假期"更干净——避免在验收标准里引入"假设无假期"的特例，使 AC 始终代表产品的真实默认行为（真实节假日参与计算）。代码侧（T1）须固化 `2026-06-24` 这一"端午窗口"正确行为测试，防止回归。
- **影响章节**：§7 AC-1。

### DR-2：技术栈偏差（采用方案 A）

- **问题**：PRD 技术栈声明含 MUI，但 `package.json` 无 `@mui/material` 依赖，组件全部用原生元素 + Tailwind 实现（Tailwind only）。
- **决策**：**接受 Tailwind only，更新 PRD 技术栈描述为「Vite + React + Tailwind CSS」**，不再要求引入 MUI。
- **理由**：现有实现已通过构建与测试，引入 MUI 属无收益重构（架构师一致意见）。保持现状可降低风险、缩短工期，且纯前端 + Tailwind 已满足 NFR（可用性 / 移动端自适应）。
- **影响章节**：表头「技术栈」字段；本决策记录尾部备注。

### DR-3：反向计算语义（首版同时保留两种能力）

- **问题**：现有代码的"反向"是"起始 − N 天"（`direction='backward'`，已知起点与 N 求终点）；PRD P1-1 描述的是"给定截止日反推剩余天数"（已知终点反推剩余）。需明确首版范围。
- **决策**：**首版同时交付两种反向能力，但严格区分语义**：
  1. **截止日反推（P1-1 核心语义）**：`countDaysBetween(start, deadline, type)`，反推"还需多少工作日 / 日历日"。因与 P0-4「剩余日历天数倒计时」逻辑同源、用户价值明确（倒计时类故事），纳入首版 P1 范围（对应任务 T6，依赖 T2）。
  2. **反向偏移（已有能力，保留）**：`direction='backward'`，求"起始日 − N 天"，作为"往前推 N 天"的独立工具保留，不与上述混淆。
- **理由**：两者服务不同场景，现有 backward 已实现且无害，保留零成本；P1-1 是 PRD 原始意图且紧贴核心目标（降低认知负担 / 倒计时），不应推迟。关键是界面与文档必须区分命名，防止用户误用。
- **影响章节**：§3 P1-1 需求描述。

---

> 备注：本文档技术栈已据 DR-2 更新为 **Vite + React + Tailwind CSS**（纯前端）；MUI 不再纳入范围。后续如主理人另行要求引入 MUI 重构组件，须作为单独技术债任务评估。
