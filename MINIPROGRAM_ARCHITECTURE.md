# 微信小程序架构设计：时间计算器（time-calculator-mini）

> 本文档为「时间计算器」Web SPA 的**微信小程序版**架构设计。目标：复用既有计算内核、以最低成本上线、快速获取微信生态用户。
>
> 配套工程位于仓库 `miniprogram/` 子目录，可直接用「微信开发者工具」打开并上传。

---

## 1. 背景与定位

| 维度 | 说明 |
| --- | --- |
| 来源产品 | 现有 Vite + React + TS 的 Web SPA（工作日/节假日计算器） |
| 小程序定位 | 同一计算能力的**原生微信小程序版**，覆盖核心「期限推算」与「间隔反推」 |
| 目标用户 | 法务/合同、项目排期、HR、财务等与「N 个工作日后是几号」强相关的微信用户 |
| 上线策略 | 复用已验证的计算内核 → 原生小程序外壳 → 微信开发者工具上传 → 审核发布 |

### 为什么用原生小程序而非跨端框架
- 微信审核对原生结构更友好，包体更小（首屏更快），无需额外编译链。
- 计算内核是**纯函数**，与 UI 框架解耦，原生化成本极低、风险小。
- 后续若需跨端（H5/App），内核可直接复用，UI 层替换即可。

---

## 2. 总体架构

```
┌─────────────────────────────────────────────┐
│                微信客户端（用户）              │
└───────────────┬─────────────────────────────┘
                │ 打开 / 扫码 / 搜索
                ▼
┌─────────────────────────────────────────────┐
│           微信小程序运行时容器                 │
│  ┌───────────────────────────────────────┐  │
│  │  app.js (全局：地区/配置/缓存初始化)     │  │
│  │  app.json (pages / tabBar / window)     │  │
│  └───────────────────────────────────────┘  │
│       │                              │        │
│       ▼                              ▼        │
│  ┌─────────────┐            ┌──────────────┐ │
│  │ calculator  │            │  remaining   │ │
│  │  期限推算页  │            │  间隔反推页   │ │
│  └──────┬──────┘            └──────┬───────┘ │
│         │                         │          │
│         └───────────┬─────────────┘          │
│                     ▼                        │
│  ┌───────────────────────────────────────┐  │
│  │             utils/calculator.js         │  │  纯函数计算内核（无框架依赖）
│  │             utils/dateUtils.js          │  │
│  │             data/holidays.js            │  │  2024–2027 节假日/调休数据
│  │             data/regions.js             │  │  中国大陆 / 中国香港地区集
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### 架构分层
1. **表现层（Pages）**：`calculator`、`remaining` 两个 tabBar 页面，负责表单采集与结果展示。
2. **逻辑层（Logic）**：`utils/calculator.js` 暴露 `computeDeadline` / `computeRemaining`，与原 Web 版语义一致。
3. **数据层（Data）**：`data/holidays.js`、`data/regions.js` 内置节假日数据集，随包发布，无需联网。
4. **全局层（App）**：`app.js` 负责读取/写入用户偏好（地区、休息日、是否含起点）到 `wx.storage`。

---

## 3. 目录结构（实际工程）

```
miniprogram/
├── project.config.json        # 项目配置（占位 AppID：touristappid）
├── project.private.config.json # 本地私有配置（开发者工具自动维护）
├── sitemap.json               # 收录配置
├── app.js                     # 全局逻辑 + storage 初始化
├── app.json                   # 全局配置：pages / tabBar / window
├── app.wxss                   # 全局样式
├── utils/
│   ├── dateUtils.js           # 日期工具（纯函数）
│   ├── calculator.js          # 计算内核（纯函数）
│   └── format.js              # 结果展示格式化助手
├── data/
│   ├── holidays.js            # 2024–2027 节假日/调休（含 2027 预估标注）
│   └── regions.js             # 地区注册表（cn-mainland / hk）
└── pages/
    ├── calculator/            # 期限推算
    │   ├── calculator.js
    │   ├── calculator.json
    │   ├── calculator.wxml
    │   └── calculator.wxss
    └── remaining/             # 间隔反推
        ├── remaining.js
        ├── remaining.json
        ├── remaining.wxml
        └── remaining.wxss
```

---

## 4. 页面与功能映射

| 页面 | 路由 | 核心能力 | 对应 Web 版 |
| --- | --- | --- | --- |
| 期限推算 | `pages/calculator/calculator` | 起始日 + 期限长度 + 口径（工作日/日历日/自然日）+ 方向（正反）+ 含起点 + 地区 | App.tsx 主计算 |
| 间隔反推 | `pages/remaining/remaining` | 起始日 + 截止日 + 口径 → 反推间隔天数（工作日/日历日） | P1-1 反向反推 |

> 暂未移植「节假日/调休管理」「设置（单休/自定义休息日）」等次要页面，以**最快上线**为优先；后续迭代可在 `pages/` 下增补，内核已支持 `WorkCalendarConfig`。

### 计算内核复用说明
Web 版计算逻辑（`src/utils/calculator.ts` 等）为纯 TS、无 React 依赖。小程序工程将其改写为**等价纯 JS**（`utils/calculator.js` / `dateUtils.js`），保持：
- 同一套节假日判定（`isWorkday` / `isHoliday` / `isMakeupWorkday`）
- 同一套算法（`computeDeadline` 连续日/工作日分支、`computeRemaining` 反推）
- 同一份内置数据（`data/holidays.js`、`data/regions.js` 由 `src/data/*` 平移）

保证**小程序与网页计算结果完全一致**，避免口径分歧。

---

## 5. 状态与数据策略

| 项 | 方案 |
| --- | --- |
| 用户偏好（地区/含起点/休息日） | `wx.setStorageSync` 持久化，启动时 `app.js` 读取 |
| 节假日数据 | 内置静态 JS，随包发布；2027 为预估并标注，待官方发布后更新 |
| 联网需求 | 无。纯本地计算，离线可用，审核更顺畅 |
| 数据分析/获客 | 后续可接「小程序数据统计」+ 分享卡片裂变（本版预留 `onShareAppMessage`） |

---

## 6. 上线与部署流程

> ⚠️ 微信小程序**不能**像网页那样由外部服务器直接发布。必须走微信官方闭环：

1. **替换 AppID**：将 `miniprogram/project.config.json` 中的 `appid`（当前占位 `touristappid`）改为你自己在 [mp.weixin.qq.com](https://mp.weixin.qq.com) 注册的 AppID。
2. **导入工程**：微信开发者工具 → 导入项目 → 选择本仓库 `miniprogram/` 目录。
3. **本地预览**：工具内「编译」即可真机预览，验证计算正确。
4. **上传代码包**：工具内「上传」→ 填写版本号（如 `1.0.0`）与备注。
5. **提交审核**：登录 MP 后台 → 版本管理 → 开发版 → 提交审核。
6. **发布**：审核通过后「发布」，即全量上线。

### 本仓库职责
- `main` 分支保留小程序工程源码（与 Web SPA 同仓，统一管理）。
- Web SPA 仍由 `.github/workflows/deploy.yml` 自动部署到 GitHub Pages（本次新增 `miniprogram/` 不影响网页构建）。
- 小程序代码包需由你在开发者工具上传，**本环境无法直连微信后台代为发布**。

---

## 7. 获客与迭代建议（快速拿到用户）

1. **名字与搜一搜**：小程序名含「工作日」「截止日」「天数计算」等关键词，利于微信搜索自然流量。
2. **分享裂变**：结果页提供「分享给同事」卡片（已预留 `onShareAppMessage`），利用职场场景自传播。
3. **首屏快**：纯本地计算、包体小，首屏 < 1s，审核与留存友好。
4. **后续迭代**：补「节假日管理」「自定义休息日」「到期提醒订阅消息」进一步提升留存。

---

## 8. 风险与注意

- **占位 AppID 不可发布**：`touristappid` 仅用于本地预览，正式上传必须用自有 AppID。
- **2027 节假日为预估**：数据文件中已标注，官方发布后需更新 `data/holidays.js`。
- **审核合规**：计算结果类工具一般易过审；避免内置广告/外链，保持工具属性。
